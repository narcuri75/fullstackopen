``` mermaid
sequenceDiagram
  participant browser
  participant server

Note over browser: User opens /spa page
browser->>server: GET /spa
server-->browser: HTML (SPA shell)

browser->>server: GET /main.css
server-->browser: CSS file

browser->>server: GET /spa.js
server-->browser: JavaScript (SPA logic)

Note right of browser: JS runs and requests notes data
browser->>server: GET /data.json
server-->browser: JSON (list of notes)

Note over browser: JS renders notes on the page (no reloads)
```
